# ── Web Framework ─────────────────────────────────────────────────────────
fastapi==0.115.0          # el framework que construye los endpoints
uvicorn[standard]==0.30.0  # el servidor que corre FastAPI (como el motor del carro)

# ── Base de Datos ──────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36  # ORM — traduce Python a SQL y viceversa
asyncpg==0.30.0              # driver async para PostgreSQL (más rápido que psycopg2)
alembic==1.13.3              # migraciones de base de datos (control de versiones del schema)

# ── Validación de Datos ────────────────────────────────────────────────────
pydantic==2.9.2              # define y valida la forma de los datos (como contratos de datos)
pydantic-settings==2.5.2     # lee variables de entorno (.env) automáticamente
email-validator==2.2.0       # valida emails en Pydantic

# ── Seguridad ──────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0  # genera y verifica JWT (tokens de autenticación)
passlib[bcrypt]==1.7.4            # hashea contraseñas (nunca guardes contraseñas en texto plano)
python-multipart==0.0.12          # necesario para recibir forms y archivos en FastAPI

# ── Redis (Cola de Misiones) ───────────────────────────────────────────────
redis[hiredis]==5.1.1        # cliente de Redis para Python

# ── Machine Learning (Sentinel) ───────────────────────────────────────────
scikit-learn==1.5.2          # Naive Bayes Gaussiano y herramientas de ML
numpy==2.1.1                 # operaciones numéricas (base de scikit-learn)
pandas==2.2.3                # manipulación de datos tabulares

# ── HTTP Client (para Google Maps Distance Matrix API) ────────────────────
httpx==0.27.2                # cliente HTTP async (mejor que requests para código async)

# ── Utilidades ────────────────────────────────────────────────────────────
python-dotenv==1.0.1         # carga variables de entorno desde archivo .env
pytz==2024.2                 # manejo de zonas horarias (México: America/Mexico_City)

# ── Testing ────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
